#pragma once

#include <string>
#include <vector>
#include <unistd.h>
#include <pwd.h>

namespace Config {

#ifdef __APPLE__
    const std::string HOME = std::string(getpwuid(getuid())->pw_dir); // Get home directory on Unix/Linux/MacOS
    const std::string APP_DIR = HOME + "/Library/Application Support/@PROJECT_NAME@/"; // Writing into the .app bundle is not allowed as it is signed
#else
    const std::string APP_DIR = ""; // On other platforms just write into the application directory
#endif

const std::string SHADER_DIR = "shaders/";
const std::string MODEL_DIR = "models/";
const std::string MATERIAL_DIR = "materials/";
const std::string SCENE_DIR = APP_DIR + "scenes/";

#ifndef NDEBUG
//#define COMPOSE_SHADERS // Uncomment to compose shaders in DEBUG mode
                          // Currently disabled because the #line directive serves better for debugging
#endif
const std::string COMPOSED_SHADER_DIR = "@CMAKE_SOURCE_DIR@/composed/";

const std::vector<std::string> MODEL_FILES = {@MODEL_FILES@};
const std::vector<std::string> SHADER_FILES = {"shared/debug.frag", "shared/lambert.frag", "shared/ggx.frag", "shared/ggx_aniso.frag", "glints/glints.frag", "glints/reference.frag", "layered/layered.frag"};
enum class ShaderType : unsigned int { DEBUG, LAMBERT, GGX, GGX_ANISO, GLINTS, GLINTS_REF, LAYER};
const std::vector<std::string> MATERIAL_FILES = {@MATERIAL_FILES@};

const std::vector<std::string> GLINTS_DEBUG_MODES = {
    "off",              // 0
    "D",                // 1
    "Dmax",             // 2
    "DP",               // 3
    "foot.area",        // 4
    "foot.theta",       // 5
    "foot.aniso",       // 6
    "foot.major",       // 7
    "grid",             // 8
    "lodWeight",        // 9
    "anisoWeight",      // 10
    "thetaWeight",      // 11
    "centerCase",       // 12
    "baryA",            // 13
    "baryB",            // 14
    "baryC",            // 15
    "baryD",            // 16
    "seedA",            // 17
    "uvGrid",           // 18
    "uvGridCompensated",// 19
    "uvTriangles",      // 20
    "samples",          // 21
    "0",                // 22
    "1",                // 23
    "2",                // 24
};

const std::vector<std::string> LAYERED_DEBUG_MODES = {
    "off",                      // 0
    "BRDF",                     // 1
    "Energy of lobe 0",         // 2
    "D of lobe 0",              // 3
    "G of lobe 0",              // 4
    "Alpha input of lobe 0",    // 5
    "Roughness of layer 0",     // 6
    "Energy of lobe 1",         // 7
    "D of lobe 1",              // 8
    "G of lobe 1",              // 9
    "Alpha input of lobe 1",    // 10
    "Roughness of layer 1",     // 11
    "Energy of lobe 2",         // 12
    "D of lobe 2",              // 13
    "G of lobe 2",              // 14
    "Alpha input of lobe 2",    // 15
    "Roughness of layer 2",     // 16
    "Fresnel of layer 0",       // 17
    "Fresnel of layer 1",       // 18
    "Fresnel of layer 2",       // 19
    "Debug",                    // 20
};

const unsigned int MAX_LAYERS = 4;
const size_t NUMBER_OF_MEASUREMENTS = 60;
}